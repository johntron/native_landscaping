{"_type":"issue","id":"nl-5go.1","title":"Un-pin / condense the sticky toolbar on mobile","description":"styles.css:56 sets .view-toolbar { position: sticky; top: 0 }. Measured 547px tall at 412px wide = 60% of a 915px viewport and ~70% of a real Pixel 10's ~780px visible area. Confirmed pinning at top:0 across scrollY 400-1200, i.e. the entire range where any view is on screen. Usable height below it is 368px measured, ~233px on device - less than the 278px height of a single .view. The app's only content cannot fit on screen.\n\nIt stacks, vertically, at 412px: legend (3 items), 5 layer chips wrapping to 3 ragged right-aligned rows (105px), 2 pill switches + Undo/Redo, month label+slider+readout, scale label+slider+number input, and a 3-bar 'Scale reference' card with a hint line.","design":"Options:\n- Drop position:sticky below ~960px, or keep only a ~56px condensed bar (month scrubber + a 'Controls' button) pinned and move the rest into a bottom sheet / \u003cdetails\u003e drawer.\n- The month slider is the core interaction and the one control worth permanent space. Scale, layers, labels, lock, undo/redo and the legend are set-once-then-forget.\n- Move the legend and 'Scale reference' card below the views or into the drawer.\n- Collapse the scale row: #scaleInput duplicates #scaleSlider, and each view already renders its own '1 ft = N px' badge (.view-scale).","acceptance_criteria":"At 412x915, a complete .view is visible on screen at the same time as whatever toolbar remains pinned.","status":"closed","priority":0,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:01Z","created_by":"John Syrinek","updated_at":"2026-08-19T19:10:35Z","started_at":"2026-08-19T19:07:16Z","closed_at":"2026-08-19T19:10:35Z","close_reason":"view-toolbar drops position:sticky below 960px, verified in 412x915 iframe","dependencies":[{"issue_id":"nl-5go.1","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:00Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-5go.2","title":"Tap a plant to open a species detail sheet","description":"On mobile there is no way to see species details by touch. Provable from source; a resized desktop browser has hover and will falsely appear to work.\n\n- Details are delivered ONLY through native SVG \u003ctitle\u003e tooltips (src/render/tooltip.js -\u003e renderTopView / renderElevationView). Confirmed 192 \u003ctitle\u003e elements for 192 plant groups. \u003ctitle\u003e never fires on touch.\n- styles.css reinforces this with '.view svg [data-name]:hover { cursor: pointer }'.\n- Species table cross-highlighting is bound to mouseenter/mouseleave (src/app.js:734-735) - hover-only.\n- Clone is bound to contextmenu (src/app.js:522), and a document click handler at :541 hides the menu. Android long-press over SVG should be verified on-device rather than assumed, but there is no discoverable tap affordance either way.\n\nNet: tapping a plant on a phone does nothing. The species table at the bottom of a 7-screen page is the only route to details and is disconnected from the plant you tapped.","design":"Add a click/pointerup handler on [data-plant-id] that opens a bottom sheet rendering buildTooltipLines(plant, state), plus a 'Clone' action - this reuses the existing tooltip builder and replaces the contextmenu-only clone path with something that works on both input types. Mirror mouseenter/mouseleave on species rows with click-to-pin highlighting. Keep the \u003ctitle\u003e elements: they remain the desktop tooltip and the accessible name.\n\nBlast radius (codegraph impact buildTooltipLines): tooltip.js, topView.js, elevationViews.js, renderViews.js, tests/topView.test.js, tests/elevationViews.test.js.","acceptance_criteria":"Tapping a plant in any of the three views opens a detail sheet with the same content the desktop tooltip shows, and offers Clone. No regression to the existing \u003ctitle\u003e tooltips.","status":"closed","priority":0,"issue_type":"feature","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:01Z","created_by":"John Syrinek","updated_at":"2026-08-19T19:19:33Z","closed_at":"2026-08-19T19:19:33Z","close_reason":"tap [data-plant-id] opens a bottom detail sheet (buildTooltipLines content) with a Clone action; verified via headless iframe at 412x915, existing SVG \u003ctitle\u003e tooltips unaffected","dependencies":[{"issue_id":"nl-5go.2","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:01Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-5go.3","title":"touch-action:none while unlocked swallows page scroll over every view","description":"src/interaction/dragController.js:170 and :349 set svg.style.touchAction = locked ? 'auto' : 'none'. Verified: with positions unlocked, all three view SVGs compute touch-action:none.\n\nEach view is 370x278 inside a 412px column, so any vertical swipe that STARTS on a drawing is swallowed and the page will not scroll. With three views stacked, most of the scrollable area is dead. This is the single most maddening mobile behavior in the app, and is half the reason the scrolling feels 'nested'.\n\nLocked is the default (lockToggle checked, no localStorage on first visit), so a first-time visitor will not hit this - but it makes editing on a phone effectively impossible.","design":"Either:\n- use touch-action: pan-y instead of none while unlocked, so vertical page scroll survives and drags read as horizontal/hold gestures; or\n- require a long-press (or tap-to-select-then-drag) to begin a touch drag, keeping touch-action:auto until that gesture is recognized.\n\nBlast radius (codegraph impact createPlantDragController): dragController.js, src/app.js init.","acceptance_criteria":"With positions unlocked on a touch device, a vertical swipe starting on a drawing scrolls the page, and dragging a plant still works.","status":"closed","priority":0,"issue_type":"bug","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:01Z","created_by":"John Syrinek","updated_at":"2026-08-19T19:07:45Z","started_at":"2026-08-19T19:07:16Z","closed_at":"2026-08-19T19:07:45Z","close_reason":"touch-action: pan-y instead of none, preserves vertical page scroll while unlocked","dependencies":[{"issue_id":"nl-5go.3","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:01Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-0di","title":"A dragged photo is stretched to its panel instead of keeping its aspect","description":"An uncalibrated photo is painted 'background-size: contain', so it letterboxes and is scaled uniformly. The first drag materializes photoFt from the fallback in photoRectFt (src/render/setupOverlay.js) — the PANEL's rectangle — and placementToCssBackground then emits '100% 100%', which stretches the image to a shape it does not share. Backyard's 800x600 photos into a 33.63x26.22 ft panel is 3.9% off; a 16:9 upload into the same panel is 39% off, and a plant at the right height in feet then sits at the wrong height against the photo.\n\nThe panel used to BE the photo's shape, which is why this could not happen before views were derived from the yard.\n\nFix: materialize photoFt from the rectangle the photo actually occupies under 'contain', which needs the image's intrinsic aspect. The browser has it once the image loads; nothing currently reads it. Storing photoFt at upload time, from the decoded bitmap the upload path already holds, would give every photo a real rectangle before it is ever dragged.","status":"closed","priority":1,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-22T19:48:32Z","created_by":"John Syrinek","updated_at":"2026-08-22T22:25:42Z","closed_at":"2026-08-22T22:25:42Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-urc","title":"A shrunken yard strands the plants and features already outside it","description":"Setting yardFt smaller than the content in it leaves plants and features outside the yard with nothing saying so. Live right now in projects/example-frontyard, saved from the app at 6 x 25 ft: plant 'fournerve-edge' sits at (9.07, 9.08) and all three features reach past x 6 or y 25 — the box out to y 31.5. resolveYardBounds clamps NEW drags to the yard, so those cannot be dragged back; they draw outside it or not at all.\n\nThis is the half of the silent-shrink problem nl-t08 did not remove. It removed view-vs-view disagreement — no view can miss the yard now — but the yard itself is still a number a person can type below what is standing in it.\n\nFix: the Setup panel's yard section counts what falls outside the candidate yard on every edit, before Save, the way the old 'Shared yard' section reported the narrowing. Naming the plants is better than counting them. Refusing the edit is wrong — shrinking a yard you have not planted yet is normal.","status":"open","priority":1,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-22T19:48:32Z","created_by":"John Syrinek","updated_at":"2026-08-22T19:48:32Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-k1k","title":"Features panel lists nothing until an edit: panel not re-rendered after async feature load","description":"appState.features is assigned after loadProjectFeatures in src/app.js, but featurePanel.render() only ran earlier with an empty list. The shapes draw (render() follows), yet the Yard features list says 'Nothing drawn yet' until any commit re-renders the panel. Most visible on mobile, where the list is the main affordance.","status":"closed","priority":1,"issue_type":"bug","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T20:14:49Z","created_by":"John Syrinek","updated_at":"2026-08-21T20:15:36Z","started_at":"2026-08-21T20:14:52Z","closed_at":"2026-08-21T20:15:36Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-cgn","title":"example-frontyard's plan view is framed in a stale coordinate origin","description":"The boot notice says view \"west\" covers y 0-14.3 ft while the plan covers 14.8-45.1 ft. nl-9j6 reported it and deliberately did not repair it. Evidence now settles which number is wrong: the plan's originFt {2, 14.833} is the outlier. Overlaying features.json's traced surface on the plan photo under a {0,0} origin puts it exactly on the dark bed; under {2, 14.833} it falls off the bottom of the photo entirely. All three elevations still carry originFt.x = 0 from the pre-photo 12x25 authoring, and the plants all sit at y = 14.833, the old bounds.y.min, i.e. clamped. Fix the plan's frame, and surface the shared yard live in Setup mode so a handle drag that reframes the plan cannot silently strand the elevations again.","acceptance_criteria":"example-frontyard raises no yard conflict; Setup mode shows the resulting shared yard against the plan as the author edits; unit tests use inlined fixtures, never projects/.","status":"closed","priority":1,"issue_type":"bug","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T13:24:05Z","created_by":"John Syrinek","updated_at":"2026-08-21T13:29:44Z","started_at":"2026-08-21T13:24:11Z","closed_at":"2026-08-21T13:29:44Z","close_reason":"Fixed by reframing the plan, not the elevations. projects/example-frontyard/project.json's plan carried originFt {2, 14.833} while all three elevations kept originFt.x = 0 and features.json's one hand-traced surface was 0-based; overlaying that surface on the plan photo settled it — under {0,0} it lands exactly on the dark bed, under {2, 14.833} it falls off the bottom of the photo. The plan's originFt is now dropped (serializeProjectConfig omits {0,0}), elevation origins, features.json and planting_layout.csv untouched. Prevention: describeYardBounds() in src/render/yardBounds.js reports the shared yard, the plan's rectangle, and which view holds each differing edge; the Setup panel renders it under 'Shared yard' on every edit, in red when a view overlaps the plan nowhere — so the gesture that strands an elevation says so as it happens, not at next boot. 5 unit tests with inlined fixtures, including example-frontyard's own numbers. Residual narrowing filed separately.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.3","title":"Renderers and drag controllers consume viewTransform; Scale slider becomes zoom","description":"Switch src/render/topView.js, src/render/elevationViews.js, and src/interaction/dragController.js off the global pixelsPerInch and onto createViewTransform(view).\n\npixelsPerInch stops being a render input entirely. The Edit-mode Scale slider becomes ZOOM — panel display size only, no effect on plant coordinates or background alignment. SCALE_LIMITS in src/constants.js is repurposed as zoom limits. #scaleIndicator's '3 in / 1 ft / 10 ft' bars read from the selected view's pxPerFt.\n\nDrag clamping switches from viewBoxToFeet(viewBox.width, pixelsPerInch) to originFt + extentFt.","acceptance_criteria":"With the migrated backyard project, plants render at pixel-identical positions to before. Dragging the Scale slider zooms panels while plants stay glued to the background photo. tests/topView.test.js and tests/elevationViews.test.js updated to the new signature; npm test and npm run test:e2e pass.","notes":"normalizeProjectConfig now returns config.views[] AND a derived compat projection (config.plan / config.elevations / config.pixelsPerInch) built by legacyProjection() in src/data/projectConfig.js. That shim exists only so the app kept booting between beads — delete legacyProjection and its spread in normalizeProjectConfig once nl-tz7.3 and nl-tz7.4 have moved app.js, renderViews.js and viewConfig.js onto views[].","status":"closed","priority":1,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:29Z","created_by":"John Syrinek","updated_at":"2026-08-20T02:31:48Z","started_at":"2026-08-20T02:27:05Z","closed_at":"2026-08-20T02:31:48Z","close_reason":"Renderers and drag controllers now build geometry from createViewTransform(view); pixelsPerInch is gone as a render input. Scale slider is zoom (CSS grid track scaling only). The legacyProjection compat shim from nl-tz7.2 is deleted. npm test green; e2e 10/10; browser check confirmed panel 409-\u003e1253px with an identical plant path.","dependencies":[{"issue_id":"nl-tz7.3","depends_on_id":"nl-tz7.2","type":"blocks","created_at":"2026-08-19T20:58:25Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.3","depends_on_id":"nl-tz7.1","type":"blocks","created_at":"2026-08-19T20:58:25Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.3","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:28Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-tz7.4","title":"Config-driven view panels (no more hardcoded topView/frontView/sideView)","description":"The three panel names are baked in at more sites than the SVG refs:\n  - index.html: #topSvg / #frontSvg / #sideSvg, data-view-panel='topView|frontView|sideView'\n  - src/app.js:63-77: svgRefs.elevationSvgs and labelRefs built from literals\n  - styles.css:779-781 and 1089-1091: maximize rules ENUMERATE all three names\n  - configureViews / renderViews: positional mapping\n\nAdd a \u003ctemplate\u003e to index.html, cloned once per views[] entry inside configureViews (already idempotent pure attribute/style assignment). renderViews loops views[] dispatching on type to renderTopView / renderElevationView. Keep id='topSvg' on the first plan view so existing e2e selectors keep working.\n\nMaximize rules collapse to a generic .views[data-maximized] .view-panel.is-maximized, with refreshMaximizedView toggling the class; the data-maximized attribute stays for the container-level rules at styles.css 771/775/1100.","acceptance_criteria":"A project.json declaring four views renders four panels, each maximizable. Backyard's three views look unchanged. Live preview works without a page reload: mutating appState.project then re-calling configureViews + render() is sufficient. npm run test:e2e passes.","status":"closed","priority":1,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:29Z","created_by":"John Syrinek","updated_at":"2026-08-20T02:44:41Z","started_at":"2026-08-20T02:39:27Z","closed_at":"2026-08-20T02:44:41Z","close_reason":"Panels are cloned from a \u003ctemplate\u003e once per views[] entry; configureViews reconciles by view id so unchanged panels keep their SVG. Maximize CSS collapsed to .view-panel.is-maximized in both the desktop and mobile blocks. renderViews loops panels dispatching on type. Verified a 4-view project renders 4 maximizable panels against a scratch PUBLIC_DIR server; npm test and e2e 10/10 green.","dependencies":[{"issue_id":"nl-tz7.4","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:29Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.4","depends_on_id":"nl-tz7.2","type":"blocks","created_at":"2026-08-19T20:58:25Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"nl-tz7.2","title":"views[] schema, legacy migration, and serializeProjectConfig","description":"Replace project.json's {plan, elevations[]} with a views[] array whose geometry is authored in feet.\n\nEach view: { id, type: 'plan'|'elevation', viewFrom?, label, sublabel, viewBox {width,height}, originFt {x,y}, extentFt {width,height}, background|null, backgroundFrom? }\n\noriginFt is the yard coordinate at the viewBox bottom-left. On an elevation it replaces both pixel offsets: leftOffsetPx 80 -\u003e originFt.x = -80/pxPerFt; bottomOffsetPx 100 -\u003e originFt.y = -100/pxPerFt.\n\nnormalizeProjectConfig accepts BOTH shapes; legacy migrates with pxPerFt = defaultPixelsPerInch * 12. Writes always emit views[]. ELEVATION_SLOTS=2 goes away.\n\nNew serializeProjectConfig(config) omits values equal to defaults — normalizeProjectConfig inflates labels, sublabels and offsets, so writing the normalized object straight back would bloat every project file.\n\nnormalizeAssetPath currently THROWS on a missing background; relax it to allow null so a newly added view is valid before its image exists. Add the matching consumer branch in configureViews in the same commit — it sets container.style.backgroundImage unconditionally and would request projects/\u003cslug\u003e/null.","acceptance_criteria":"pxPerFt = viewBox.width/extentFt.width must match viewBox.height/extentFt.height within tolerance; non-uniform views are rejected with a clear message. Path-traversal guard on background still holds. tests/projectConfig.test.js covers legacy-\u003eviews[] migration, new-shape round-trip through serialize+normalize, default omission, and the validation errors; the /exactly 2 elevations/ assertion is removed.","status":"closed","priority":1,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:12Z","created_by":"John Syrinek","updated_at":"2026-08-20T02:20:35Z","started_at":"2026-08-20T02:18:03Z","closed_at":"2026-08-20T02:20:35Z","close_reason":"views[] schema + legacy migration + serializeProjectConfig landed. normalizeProjectConfig reads both shapes and also emits a derived legacy projection (plan/elevations/pixelsPerInch) so the app keeps working until nl-tz7.3/nl-tz7.4 rewire consumers. npm test (14 files) and npm run test:e2e (10 tests) both green.","dependencies":[{"issue_id":"nl-tz7.2","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:11Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"nl-tz7.1","title":"Extract viewTransform.js — shared feet\u003c-\u003epixel math","description":"src/interaction/dragController.js (buildPointerContext), src/render/topView.js, and src/render/elevationViews.js each re-derive feet\u003c-\u003epixels independently. Extract one module so the setup tool, the renderers, and the drag controllers cannot drift.\n\nNew src/render/viewTransform.js:\n  createViewTransform(view) -\u003e {\n    pxPerFt, toPx(feet), toFeet(px),\n    planToViewBox({x,y}), viewBoxToPlan({x,y}),\n    axisToX(axisFeet), xToAxis(px),\n    groundY, orientation\n  }\n\nIt WRAPS resolveElevationOrientation / elevationAxisToViewBoxX from src/render/elevationOrientation.js rather than replacing them, so the mirror/offset table stays in one place and tests/elevationOrientation.test.js keeps passing.","acceptance_criteria":"tests/viewTransform.test.js exists and covers plan + all four elevation directions, mirrored and unmirrored, with non-zero originFt. Includes the regression-parity case: today's backyard values (pixelsPerInch 2.25, viewBox 800x600, bottomOffsetPx 100, leftOffsetPx 80, viewFrom east) produce pixel coordinates identical to the current formulas. npm test passes.","notes":"Parity is covered in both directions: axisToX/planToViewBox against the renderer formulas (topView.js, elevationAxisToViewBoxX), and xToAxis/viewBoxToPlan against the drag-controller formulas (buildPointerContext.positionFeet, pointerAxisFeet). Expected values are built from legacy literals, never from the new transform, so the tests are not round-trip tautologies. 12 tests.","status":"closed","priority":1,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:11Z","created_by":"John Syrinek","updated_at":"2026-08-20T02:15:32Z","started_at":"2026-08-20T02:12:35Z","closed_at":"2026-08-20T02:14:11Z","close_reason":"src/render/viewTransform.js + tests/viewTransform.test.js added; 10 tests pass, full npm test green. Additive only — no consumers rewired (that is nl-tz7.3).","dependencies":[{"issue_id":"nl-tz7.1","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:11Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-tz7","title":"Viewport Setup Mode","description":"Add a third toolbar mode (Setup) for defining a project's views before plants are placed: create/remove/reorder views, set each view's real-world extent and origin in feet, pick backgrounds, place the elevation ground line and near edge, and cut zoomed detail crops of the plan. Config saves back to projects/\u003cslug\u003e/project.json via the persistence server.\n\nRoot problem: today project.json authors PIXELS (viewBox, bottomOffsetPx, leftOffsetPx) with feet-\u003epx coming from a global defaultPixelsPerInch that the Edit-mode Scale slider mutates at runtime. A calibration tool must not author pixels, so the schema inverts to feet-authored extentFt/originFt and the Scale slider is demoted to zoom.\n\nFull plan: ~/.claude/plans/help-me-plan-for-jiggly-dragonfly.md","design":"project.json {plan, elevations[]} collapses into views[] with type: plan|elevation. A crop is just a plan view with non-zero originFt, a smaller extentFt, and backgroundFrom pointing at the full plan view. New src/render/viewTransform.js is the keystone: one shared feet\u003c-\u003epixel module wrapping the existing elevationOrientation.js mirror table, consumed by both renderers, both drag controllers, and the new setup controller.\n\nNo new runtime dependency: the app has no bundler (jszip is a raw script-tag global), the whole pipeline emits SVG with data-plant-id attributes the e2e suite asserts on, and dragController.js already solves pointer capture / screen-\u003eviewBox-\u003efeet / rAF re-render. Canvas libs (Konva, Fabric, Paper) would fork the render model. interact.js is the fallback only if handle code balloons past ~300 lines.","notes":"STATUS 2026-08-20 (session 3). COMPLETE — every child bead is closed, including nl-tz7.8 (two-point ruler), which shipped rather than being dropped.\n\nSession 3 also repaired fixture drift: the user drove the app and saved, which rewrote projects/example-frontyard/project.json without the street-bed detail view. Restored it (9x6 ft at (3,3), backgroundFrom 'plan'); did NOT re-add rockrose-bed — the crop spec now derives the plants inside the rectangle from the CSV. See nl-2p3 for the underlying fixture/live-project conflict.\n\nThe earlier OPEN QUESTION about holly-corner sitting outside the shared yard bounds is moot: holly-corner is no longer in the layout.\n\nCommits: c5846cf (detail crops, yard bounds, add/remove plants, views[] conversion, docs) and 88ee5e1 (ruler calibration), on branch detail-crops-and-plant-editing, not yet merged to main.","status":"closed","priority":1,"issue_type":"epic","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:56:54Z","created_by":"John Syrinek","updated_at":"2026-08-20T23:00:08Z","closed_at":"2026-08-20T23:00:08Z","close_reason":"All child beads closed; Setup mode ships view CRUD, feet-authored extents, on-canvas handles, detail crops, and photo calibration.","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-5go.4","title":"'Maximize' is a no-op below 960px","description":"Clicking [data-maximize-target=\"topView\"] at 412px leaves the view at exactly 370x278 - unchanged, measured before and after.\n\nCause: the max-width:960px breakpoint already sets .views { grid-template-columns: 1fr } (styles.css:905), so hiding the other two panels frees no width, and .view { aspect-ratio: var(--view-aspect-ratio, 4/3) } locks height to width. A tall phone cannot trade its width for height.\n\nThree 'Maximize' buttons therefore occupy header space on mobile and deliver nothing.","design":"Options:\n- On mobile make maximize mean full-viewport: position:fixed; inset:0 with aspect-ratio:auto, letting the SVG letterbox. The SVG already carries preserveAspectRatio='xMidYMid meet' (src/render/viewConfig.js), so this is mostly CSS.\n- Or replace the three stacked panels with a swipeable/tabbed single view (Plan | South | East) so one drawing gets the whole column instead of a third of a 7-screen page.\n- Add pinch-to-zoom / pan inside a maximized view: at 370px wide a 30ft yard renders plants a few pixels across and the species initials are unreadable.","acceptance_criteria":"On a phone, maximizing a view makes it meaningfully larger than 370x278.","status":"closed","priority":1,"issue_type":"bug","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:25Z","created_by":"John Syrinek","updated_at":"2026-08-19T19:10:36Z","started_at":"2026-08-19T19:07:16Z","closed_at":"2026-08-19T19:10:36Z","close_reason":"maximized view goes position:fixed inset:0 with aspect-ratio:auto below 960px, verified view expands to fill viewport","dependencies":[{"issue_id":"nl-5go.4","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:24Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-5go","title":"Mobile UX overhaul (Pixel 10 class, ~412px viewport)","description":"The app is close to unusable on a phone. Measured in a 412x915 viewport against 'node server.js':\n\n- Sticky .view-toolbar is 547px tall = 60% of a 915px viewport, ~70% of a real Pixel 10's ~780px visible area. It pins at top:0 for the whole scroll range where any drawing is on screen, leaving ~233px on device vs a 278px .view height. You cannot see one complete drawing while it is pinned.\n- No tap path to species details exists at all (SVG \u003ctitle\u003e is hover-only).\n- touch-action:none while unlocked swallows vertical scroll over every drawing.\n- 'Maximize' is a no-op below 960px.\n- Only one media query exists (max-width:960px); nothing targets phone width.\n- Document is 6450px ~= 7 screens; 4615px of that is the species table.\n\nNo nested scroll containers and no horizontal overflow were found at 412px - the 'nested scroll' feeling comes from the pinned toolbar plus touch-action:none, not from a scroll container.\n\nFull review with measurements: see child issues; each carries its own numbers.","design":"Findings 2 and 3 (touch input) were argued from source, not browser automation: automated clicks are pointerType 'mouse' and a resized desktop window still has hover, so a browser check would have falsely shown them as working. Findings 1, 4, 5, 6 are browser-measured.","status":"closed","priority":1,"issue_type":"epic","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:01:36Z","created_by":"John Syrinek","updated_at":"2026-08-19T22:43:57Z","closed_at":"2026-08-19T22:43:57Z","close_reason":"All 7 mobile UX findings addressed: toolbar un-pinned, tap-to-detail sheet, touch scroll fixed, Maximize works below 960px, phone breakpoint added with clean chip wrapping, header trimmed + species cards collapsible, and tap-target/resize/scale-input polish. Verified via iframe testing at 412px and 1400px.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw.5","title":"Project-scoped persistence endpoints","description":"Scope /api/layout, /api/history, and /api/history/cursor to a project, resolving projects/\u003cslug\u003e/planting_layout.csv and layout-history.json. Validate the slug shape and confirm resolved paths stay inside the projects directory, since a client-supplied id now feeds a file path.","acceptance_criteria":"Endpoints read/write the correct project files; traversal attempts and malformed slugs are rejected with 400; omitting the project falls back to the default.","status":"closed","priority":1,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:57:06Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4aw.5","depends_on_id":"nl-4aw","type":"parent-child","created_at":"2026-08-19T08:57:05Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw.2","title":"Make viewBox, offsets, and backgrounds per-project","description":"Thread per-project viewBox dimensions and elevation offsets through configureViews, topView, elevationViews, and the bundle export instead of reading module-level constants. Move the hard-coded background URLs out of styles.css and app.js VIEW_BACKGROUNDS so they come from project config.","acceptance_criteria":"No renderer reads PLAN_VIEWBOX/ELEVATION_VIEWBOX directly for geometry; constants remain only as defaults; backgrounds are applied from project config.","status":"closed","priority":1,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:57:05Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4aw.2","depends_on_id":"nl-4aw","type":"parent-child","created_at":"2026-08-19T08:57:04Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw.3","title":"Compass-direction elevation views","description":"Generalize renderSouthElevation/renderEastElevation into one renderer driven by the compass direction the viewer stands on (north/south/east/west), deriving the horizontal yard axis, whether it is mirrored, and the front-to-back depth ordering. View labels come from project config instead of hard-coded index.html text, and the elevation drag controllers use the project-derived axis.","acceptance_criteria":"Viewing from south/east reproduces today's output exactly; viewing from north/west mirrors the horizontal axis and reverses depth ordering; labels reflect config.","status":"closed","priority":1,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:57:05Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4aw.3","depends_on_id":"nl-4aw","type":"parent-child","created_at":"2026-08-19T08:57:04Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw.1","title":"Project config schema, loader, and on-disk migration","description":"Add projects/index.json + projects/\u003cslug\u003e/project.json, a src/data/projectConfig.js loader that normalizes and validates config, and migrate the existing yard into projects/backyard/ (planting_layout.csv + img/).","acceptance_criteria":"projectConfig parses and normalizes a config with defaults; invalid slugs and missing fields are rejected; existing backyard renders unchanged from its new location.","status":"closed","priority":1,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:57:04Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4aw.1","depends_on_id":"nl-4aw","type":"parent-child","created_at":"2026-08-19T08:57:04Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw","title":"Support multiple projects","description":"Allow the app to host several landscape projects, each with its own background images, canvas dimensions, and compass orientation for the elevation views. plants.csv stays a shared global species catalog; each project has its own planting_layout.csv so its species selection is implicitly separate. Projects are authored on disk (projects/\u003cslug\u003e/) and chosen in-app via a picker that navigates to ?project=\u003cslug\u003e.","design":"On-disk convention: projects/index.json lists projects; projects/\u003cslug\u003e/project.json declares viewBoxes, background images, default scale, and the compass direction each elevation is viewed from; projects/\u003cslug\u003e/planting_layout.csv holds placements; projects/\u003cslug\u003e/img/ holds backgrounds. Switching projects reloads the page with ?project=\u003cslug\u003e rather than re-initializing app state in place. Yard axes: origin SW, x increases east, y increases north.","status":"closed","priority":1,"issue_type":"feature","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:56:48Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-t08","title":"Derive view geometry from a declared yard; photos placed, not fitted","description":"Replace per-view extentFt/originFt authoring with a project-level yardFt {width,depth} + paddingFt + elevationFt {above,below} + pxPerFt. Views derive their rect; the photo gets its own originFt/extentFt in yard feet (drag to place, ruler to scale). Drops cropped/detail views (backgroundFrom) but keeps the crop math as photo placement. Panels then share one screen scale and elevation ground lines align by construction.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-22T03:32:27Z","created_by":"John Syrinek","updated_at":"2026-08-22T04:03:47Z","started_at":"2026-08-22T03:32:31Z","closed_at":"2026-08-22T04:03:47Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-dmk","title":"app.spec 'guides appear only in setup mode' is stale: every view now draws the guides","description":"tests-e2e/app.spec.js expects exactly one [data-setup-overlay] in setup mode, but syncSetupOverlay in src/app.js now draws guides in EVERY view (only the selected one is interactive) — a deliberate change from 'Give an elevation a camera, and show every view the yard it can draw'. The assertion should be: guides in every view, handles/ruler in the selected one only. Fails at HEAD, independent of nl-k1k and the mode-pill fix.","status":"closed","priority":2,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T20:21:24Z","created_by":"John Syrinek","updated_at":"2026-08-21T20:44:32Z","closed_at":"2026-08-21T20:44:32Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-023","title":"Setup mode draws its alignment grid on one view at a time","description":"src/app.js:695 — 'The overlay belongs to exactly one view at a time, and only in Setup mode.' The grid is deliberately built to align ACROSS views (buildOverlayGeometry: 'Grid lines land on whole yard feet... so the same 5 ft line appears in the same place in every view'), and then only ever one view shows it. There is nothing to compare a view against while adjusting it, which is why getting the views into relative alignment is hard. Reported by the user 2026-08-21.","acceptance_criteria":"Every view draws the foot grid and its guides in Setup mode; drag handles stay on the selected view alone.","status":"closed","priority":2,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T14:43:11Z","created_by":"John Syrinek","updated_at":"2026-08-21T14:54:55Z","closed_at":"2026-08-21T14:54:55Z","close_reason":"src/app.js syncSetupOverlay now renders the overlay for every view in Setup mode, passing { interactive: isSelected } to renderSetupOverlay; handles are drawn only when interactive, and a reference view's group is at 0.55 opacity. Pointer capture, the ruler, and setLocked are unchanged — still the selected view alone. The grid was already computed on whole yard feet for exactly this purpose; nothing about the geometry changed, it was just never shown on more than one view.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-bkl","title":"An elevation has no observer, so a wall behind the camera is drawn in front of the yard","description":"example-frontyard's wall sits at y 21.6, north of the bed. In the south elevation (viewFrom south, farIsHigh true) it correctly sorts far and lands in the background. In the north elevation (viewFrom north, farIsHigh false) it sorts NEAR and is drawn over the plants as a 6 ft band — but the north photo was taken from the walkway south of that wall, so the wall is behind the camera and should not be drawn at all.\\n\\nThe cause is that an elevation models no observer position. Its axis span is authored (originFt.x + extentFt.width) and its ground height is authored (originFt.y), but nothing says where along the DEPTH axis the camera stands, so the camera is implicitly at infinity and everything in the yard is in front of it. Affects every wall and box, in any project, whenever a feature sits between the camera and the yard.\\n\\nNot a per-feature visibleFrom flag: features are one model projected into every view, and a per-view visibility flag on the feature breaks that.","acceptance_criteria":"An elevation may declare where its camera stands along the depth axis; a feature entirely behind it is not drawn; an absent declaration culls nothing; plants are clamped out of that region by resolveYardBounds rather than culled.","status":"closed","priority":2,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T14:43:11Z","created_by":"John Syrinek","updated_at":"2026-08-21T14:54:56Z","closed_at":"2026-08-21T14:54:56Z","close_reason":"An elevation may now declare viewerAtFt: where its camera stands, in yard feet, on its DEPTH axis. Absent means cull nothing, so every existing project draws what it drew before; zero is a real position, so the tests are on finiteness. Normalized/serialized in src/data/projectConfig.js (elevations only), carried on the transform, authored in Setup mode as 'Camera stands at (\u003caxis\u003e ft)' with a blank = anywhere. isBehindViewer in src/render/elevationOrder.js culls a feature whose depthFt.FAR sorts nearer than the observer — far, not near, so a fence the camera stands in the middle of is kept rather than half-vanishing — and it goes through compareElevationDepth so north/west (camera at the high end) come out right without a second case. Plants are never culled: resolveYardBounds narrows the depth axis to the observer instead, so a plant cannot be dragged behind a camera and the narrowing shows in the Shared yard readout. Verified in the running app on example-frontyard: with north's camera at y 21 the y-21.6 wall leaves the north elevation and still backs the south one. 12 unit tests across featureViews, yardBounds and projectConfig, all inlined. NOT written into projects/example-frontyard/project.json — the real camera position is a measurement the author has to make, and that file has uncommitted edits of theirs.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-jqd","title":"Setup-mode Save can drop a view's originFt, moving its ground line","description":"projects/example-frontyard/project.json was found with every originFt removed — including the elevations' negative originFt.y, which positions each ground line. Dropping them moves every ground line to the panel's bottom edge. The file is written only by POST /api/project (Setup mode's Save views), so a Save wrote zeros where the loaded config had real offsets.\n\nserializeProjectConfig omits originFt when it is {0,0}, which is correct on its own — the bug is upstream, wherever the in-memory view lost its origin. Suspects: a plan corner-handle drag (resolveHandleDrag rewrites originFt and extentFt together), the ruler calibration patch, or a view edit through the setup form that rebuilds a view without carrying originFt across.\n\nReproduce: load a project whose elevations have a negative originFt.y, enter Setup mode, edit or drag something, press Save views, and diff project.json. Found 2026-08-20 while working nl-avt.6; the file was restored in commit 9ac0069.","acceptance_criteria":"A Setup-mode Save round-trips a view's originFt unchanged unless the user actually moved it; a regression test covers a project with a negative originFt.y.","status":"closed","priority":2,"issue_type":"bug","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T03:57:54Z","created_by":"John Syrinek","updated_at":"2026-08-21T04:39:35Z","started_at":"2026-08-21T04:35:46Z","closed_at":"2026-08-21T04:39:35Z","close_reason":"NOT REPRODUCED as a code defect, and now pinned by tests so it cannot become one silently. tests-e2e/setupSave.spec.js drives a scratch copy of example-frontyard's exact geometry (plan origin {2, 14.83}, north originFt.y -7.14, west -3.20) through: a plain Save views; a form edit on the plan then Save; a form edit on an elevation then Save; and a ground-line handle drag then Save. Every origin round-trips untouched, and the drag moves ONLY the dragged view's origin. The pure round trip normalizeProjectConfig -\u003e serializeProjectConfig -\u003e normalize is stable too, verified separately.\n\nSo the flattening was almost certainly a real Setup-mode handle drag: dragging an elevation's ground line to the panel's bottom edge sets originFt.y to exactly 0, and serializeProjectConfig then omits an origin of {0,0} — the tool working, leaving a file that looks like it never had an origin.\n\nThe residual risk is real but different from what this bead assumed, and is NOT a serialization bug: view geometry has no undo (project.json keeps no history, deliberately — see nl-avt.5 and persistProjectConfig's comment), so one stray handle drag plus a Save silently destroys a calibrated ground line with no way back. Worth its own bead if it bites again; the cheap mitigations are a confirm on Save when an origin goes to zero, or keeping the previous project.json as a single .bak on write.\n\nFiled originally after I swept the damaged file into commit 9a63b3a with 'git add -A'. Restored in 9ac0069.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-avt.5","title":"Persist features through POST /api/features","description":"Server endpoint plus the client persistence module, following /api/project exactly: id from the directory rather than the body, resolveProjectPaths for containment, writeJsonAtomic so a crash never leaves a half-written file, and a body that must carry features[] or be refused outright (there is no history to recover from). No undo stack — features are setup, like project.json.","acceptance_criteria":"Saving features writes projects/\u003cslug\u003e/features.json atomically and only inside that project; a body without features[] is rejected; server.js changes are deployed with 'docker compose restart web'.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:53Z","created_by":"John Syrinek","updated_at":"2026-08-21T03:09:31Z","started_at":"2026-08-21T03:03:41Z","closed_at":"2026-08-21T03:09:31Z","close_reason":"server.js GET+POST /api/features, featuresFile in projectPaths.js, loadProjectFeatures/persistFeatures in src/data/persistence.js, boot wiring in app.js, tests/persistFeatures.test.js (5) + tests-e2e/features.spec.js (5, own scratch project 'features-save'). Full unit suite passes; 42/42 e2e. DEPLOYED and verified live: 'docker compose restart web' then curl 'http://127.0.0.1:8080/api/features?project=backyard' -\u003e {\"features\":[]} HTTP 200. Design calls: (1) There is a GET twin, not just the POST the bead asked for, and the app loads through it — fetching features.json off disk logs a console 404 on every load of every project that has never drawn a feature, which app.spec.js:26's console-clean assertion catches. featureConfig.loadFeatures is now static-file-mode only. (2) Missing file reads as {features: []}; a file that exists but is unreadable still throws — absent is normal, corrupt is a fault. (3) POST refuses a body with no features[]: absent means 'this is not a feature list', not 'no features', and accepting it would erase the whole yard model with no history to recover from. An explicit empty array IS accepted — that is how the last feature goes. (4) The response is re-normalized on the client: the drawing reads style and heightFt off every feature, and a response is trusted no more than a file. (5) The e2e containment test posts a body naming a different project and asserts the named one is untouched — worth noting normalizeFeatures never reads raw.id at all, unlike normalizeProjectConfig, so there is no id scrub; the test pins that rather than assuming it. PNG export needed no work: viewCapture.js clones the live SVG (cloneNode + serializeToString), so features come along free. persistFeatures has NO UI caller yet by design — nl-avt.6 owns that. Docs: AGENTS.md gained a 'Yard features' section and CLAUDE.md was mirrored per the repo rule. Non-blocking for .6: app.js passes no status callback to loadProjectFeatures, so a corrupt features.json degrades to an empty yard with only a console.warn — the author sees no features and no reason why.","dependencies":[{"issue_id":"nl-avt.5","depends_on_id":"nl-avt.1","type":"blocks","created_at":"2026-08-20T20:53:54Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-avt.5","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:52Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-avt.6","title":"Plan-only shape editor: select, move, vertex drag, add, delete","description":"The single biggest scope saver: features are edited in the PLAN view only. Footprints live in plan space, and a height is a number in a form field, not a drag — so elevations stay derived and read-only.\n\nReuse createSetupController's proven pattern rather than inventing a second interaction model: pointer capture, rAF-throttled repaint, MIN_HITBOX_RADIUS_PX for touch, and a candidate reported through onChange for the app to validate before it becomes live. MVP is select, move, drag a vertex, add a rect or line, delete, reorder — then stop. This is an app inside the app and it is where scope quietly triples.","acceptance_criteria":"A feature can be drawn, moved, reshaped, and deleted in the plan; every elevation updates from the same model; a rejected edit leaves the drawing on the last good state.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:53Z","created_by":"John Syrinek","updated_at":"2026-08-21T03:23:34Z","started_at":"2026-08-21T03:13:00Z","closed_at":"2026-08-21T03:23:34Z","close_reason":"src/render/featureOverlay.js (pure geometry: handles, hit-test, drag resolution, new shapes, reorder), src/interaction/featureController.js (pointer), src/interaction/featurePanel.js (list + form), plus a fourth mode in app.js/index.html/styles.css. tests/featureOverlay.test.js (9) + 3 UI tests in tests-e2e/features.spec.js against their own scratch project 'features-edit'. Full unit suite passes; 45/45 e2e with every pre-existing spec UNMODIFIED. No deploy needed: server.js is untouched and src/index.html/styles.css come off the bind mount.\n\nDesign calls: (1) A FOURTH mode, not a modifier on Setup. setRuler is a modifier on one gesture; feature editing has its own selection state and would have made three consumers competing for pointerdown on one SVG. applyMode decides every lock state in ONE place so exactly one kind of controller is ever unlocked. (2) Feature controllers are built ONLY for plan panels (null for elevations, index alignment kept) — a controller on an elevation would be a consumer that never unlocks. (3) Handles are drawn in EVERY plan view, not just one selected view. That differs deliberately from Setup mode's one-view rule: setup edits a single view's geometry, features edit a model shared by all of them, so editing from a detail crop is coherent. The pointer goes through transform.viewBoxToPlan, so a drag in a crop still writes YARD feet — unit-tested. (4) createFeatureShape gives a new wall/box a real heightFt. normalizeFeatures refuses one without a positive height, and the first frame of a new fence must not be what trips a guard meant for malformed files. (5) applyFeatureEdit normalizes the candidate DIRECTLY rather than serializing first — a freshly added shape has no style yet, and normalizeFeatures is what supplies one. (6) NO auto-save: features are setup like project.json, per this epic's own framing, so Save features is explicit. A plant drag auto-saves; this deliberately does not. (7) The panel got its OWN BEM block (feature-panel__*). Reusing setup-panel__* made four existing e2e specs fail on strict-mode ambiguity — two panels, one class name, and a selector for one silently matches the other. styles.css carries both hooks on every shared rule so the visual language stays single-sourced.\n\nKnown limits, none blocking: a polygon collapsed to zero area cannot be re-picked on canvas (pickFeatureAt ignores the touch radius for polygons, using pointInPolygon) — the panel list is the escape hatch. Type is not editable on an existing feature: surface/box are a polygon and wall is a path, so switching would need the footprint rebuilt; delete and re-add. Vertices can be dragged but not added or removed, so a bed stays a quadrilateral — worth its own bead if curved beds matter. nl-jfm updated: svg.style.cursor now has three writers.","dependencies":[{"issue_id":"nl-avt.6","depends_on_id":"nl-avt.3","type":"blocks","created_at":"2026-08-20T20:53:54Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-avt.6","depends_on_id":"nl-avt.5","type":"blocks","created_at":"2026-08-20T20:53:55Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-avt.6","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:53Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-avt.1","title":"features.json schema, normalize, and serialize","description":"Yard features are geometry in FEET shared across every view, so they get their own file rather than living in project.json. Three primitives: surface (flat, zero height), wall (extruded path), box (extruded polygon). Styles are authored in feet (strokeWidthFt) so a feature keeps its weight when a view is rescaled. Mirror projectConfig.js: reject non-finite coordinates, supply style defaults, drop defaults on write.","acceptance_criteria":"normalizeFeatures rejects malformed input loudly and round-trips through serializeFeatures unchanged; ids are validated slugs; unit tests cover each primitive.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:30Z","created_by":"John Syrinek","updated_at":"2026-08-21T02:43:43Z","started_at":"2026-08-21T02:38:26Z","closed_at":"2026-08-21T02:43:43Z","close_reason":"src/data/featureConfig.js + tests/featureConfig.test.js. Design calls made here that .2/.3/.5 inherit: (1) heightFt THROWS for wall/box rather than defaulting — a zero-height wall renders as nothing in an elevation, the exact silent failure 'reject loudly' exists to catch; a surface may not carry a heightFt at all. (2) Absent file == empty list: normalizeFeatures(null|{}) -\u003e {features: []}, no 'declares no features' throw, so an empty POST in .5 is valid. (3) normalize/serialize are exact inverses ({features} both ways, no projectId echo); the round-trip is tested as normalize(serialize(normalize(x))) deepEqual normalize(x), since serialize drops defaults. Both directions read one defaultStyle(type) table. (4) Only the geometry key matching the type is emitted — footprintFt for surface/box, pathFt for wall — so projection in .2 cannot read the wrong one. (5) style.fill/stroke restricted to hex, a colour name, or none, since they land in SVG attributes and arrive over an endpoint in .5. Also added projectFeaturesPath() and loadFeatures(); featuresFile in projectPaths.js is left to .5.","dependencies":[{"issue_id":"nl-avt.1","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:29Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"nl-avt.2","title":"Project a feature into plan and elevation views","description":"Pure projection module. Plan: footprint vertices through transform.planToViewBox. Elevation: footprint projected onto the view's horizontal axis via axisToX, vertical from heightToY(baseFt) to heightToY(baseFt+heightFt), producing a crude silhouette rectangle. Also reports the feature's extent along the view's DEPTH axis for sorting.\n\nEverything goes through viewTransform and resolveElevationOrientation. Doing the subtraction in feet by hand is wrong for the mirrored north/west views and wrong on the vertical axis everywhere.","acceptance_criteria":"A parity test per compass direction, mirrored included, matching the style of tests/elevationOrientation.test.js. No trigonometry or axis arithmetic outside viewTransform/elevationOrientation.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:30Z","created_by":"John Syrinek","updated_at":"2026-08-21T02:53:03Z","started_at":"2026-08-21T02:49:29Z","closed_at":"2026-08-21T02:53:03Z","close_reason":"src/render/featureProjection.js + tests/featureProjection.test.js (projectFeature dispatches; projectFeatureToPlan/ToElevation do the work). All four compass directions are pinned by absolute expected pixels, not only by mirror parity, so a bug shared by both sides of a pair cannot hide. Design calls .3 inherits: (1) The axis span is taken in PIXELS after axisToX, never in feet — on a mirrored view the larger axis value gives the smaller x, so a min/max in feet puts the rectangle's left edge on its right. (2) TWO degenerate silhouettes are legitimate, not errors: a surface has height 0 by definition, and a wall seen end-on (a fence running away from the viewer) has width 0. The renderer must draw both at strokeWidthPx or a fence vanishes in two of four elevations. (3) depthFt reports {min,max,near,far}; near/far come from orientation.farIsHigh so compareElevationDepth composes with the plant sort. BUT near is NOT a sufficient sort key on its own: a house spanning y 12..20 sorted at near=12 draws in front of a plant at y=15 that stands inside its footprint. Single-key sorting cannot be right for an extended footprint — .3 has to split or clamp. (4) closed=false for wall only; closing a fence would fill in the yard behind it.","dependencies":[{"issue_id":"nl-avt.2","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:30Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-avt.2","depends_on_id":"nl-avt.1","type":"blocks","created_at":"2026-08-20T20:53:53Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-avt.3","title":"Render features, interleaved with plants by depth in elevations","description":"Plan: features beneath plants, array order is z-order. Elevation: features and plants sorted TOGETHER far-to-near, so a fence in front of a plant hides it — the payoff that makes this epic worth building.\n\nThis is the one part that is not purely additive: renderViews currently sorts plants alone (orderTopViewPlantStates for plan, elevationViews for the rest) and must sort one combined list for elevations. Layering per view, bottom to top: sky colour, photo (if visible), ground fill, features+plants, overlays. Features draw ABOVE the photo so tracing works.","acceptance_criteria":"A feature nearer the viewer than a plant occludes it in the elevation; the same two render in the documented order in the plan; existing plant render tests still pass.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:30Z","created_by":"John Syrinek","updated_at":"2026-08-21T03:01:52Z","started_at":"2026-08-21T02:54:45Z","closed_at":"2026-08-21T03:01:52Z","close_reason":"src/render/featureViews.js (shapes), src/render/elevationOrder.js (the combined sort), + wiring in topView/elevationViews/renderViews and tests/featureViews.test.js. tests/topView.test.js and tests/elevationViews.test.js pass UNMODIFIED; 37/37 e2e. Design calls: (1) Plants and features go through ONE comparator with ONE depth accessor — a parallel per-kind expression is how features silently sort at depth 0, pinned to the very back or very front, which looks almost right. The test asserts the straddle order AND notDeepEqual against the depth-zero ordering. (2) An extended footprint is drawn WHOLE at its nearest edge, no splitting: correct for a box (nothing is planted inside a house), and the one case it gets wrong is a wall running away from the viewer, which is edge-on and therefore a hairline. (3) Bands: 0 = surface (flat, so it is ground and draws beneath every plant), 1 = plants and wall/box, 2 = the pre-existing Callirhoe force-front, moved verbatim from elevationViews. (4) SVG draws neither a zero-width nor a zero-height rect AT ALL, so both degenerate silhouettes become a \u003cline\u003e at stroke weight — without that a surface and an end-on fence are simply invisible. (5) No data-name on feature groups: styles.css gives '.view svg [data-name]:hover' a pointer cursor, and features are not interactive until .6, which hit-tests data-feature-id.\n\nHANDOFF TO nl-avt.5: the app is wired end to end EXCEPT the load. renderViews takes options.features and appState.features exists, so .5 is a one-line assignment — but .5 must add a GET /api/features twin returning {features: []} for a missing file. Fetching features.json straight off disk is NOT viable at boot: Chrome logs the network 404 to the console itself, before any try/catch runs, and tests-e2e/app.spec.js:26 asserts boot is console-clean. That was caught by running the e2e suite. loadFeatures' 404-to-empty branch is now only for pure static-file mode.","dependencies":[{"issue_id":"nl-avt.3","depends_on_id":"nl-avt.2","type":"blocks","created_at":"2026-08-20T20:53:54Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-avt.3","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:30Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"nl-e6c","title":"Add and remove plants in a layout","description":"Edit mode can only move or clone existing plants. Add a way to place a new plant from the shared species catalog (plants.csv) and a way to delete a plant from the layout, both routed through the existing history/persistence commit path.","status":"closed","priority":2,"issue_type":"feature","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T18:03:17Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:12:46Z","started_at":"2026-08-20T18:03:36Z","closed_at":"2026-08-20T18:12:46Z","close_reason":"Add plant picker in Edit mode; Remove on the detail sheet and right-click menu. Unit + e2e green.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.12","title":"Decide whether the yard has an extent separate from what a view covers","description":"Since nl-tz7.3, drag clamping uses each view's own originFt + extentFt. That fixed an upper-bound bug (the east elevation used to allow y=29.63, which rendered at 880px on an 800px viewBox) but introduced a lower bound below zero: the east elevation's originFt.x is -2.96, so a plant can be dragged to y=-2. The plan view starts at originFt {0,0}, so that plant renders at cy=654 on a 600px viewBox and disappears from the plan.\n\nViews legitimately extend past the yard — an elevation's near-edge inset is margin, not plantable ground. So a view's extent is the wrong thing to clamp plant positions to. Either clamp every view to a shared yard extent, or accept per-view clamping and make the plan view show out-of-bounds plants somehow.\n\nThis is the same question nl-tz7.6 / nl-tz7.7 must answer when the setup UI lets someone move a view's origin under existing plants.","design":"RESOLVED: clamp every view to a shared yard extent, not to the view's own rectangle. src/render/yardBounds.js resolveYardBounds(views) = the first plan view's rectangle, intersected with the axis range of the first elevation for each compass direction; later views of either kind are detail callouts and do not constrain (a plant outside a zoomed detail is not lost). An empty intersection falls back to the plan view rather than an inverted clamp range. Both drag controllers take an optional getBounds and fall back to the per-view extent when it is absent, so they still work standalone. Rejected alternative: keep per-view clamping and mark out-of-bounds plants in the plan — it leaves the plan lying about where a plant is. Checked both repo layouts against the new bounds: projects/backyard is entirely inside (bounds come out identical to the plan extent, and the old y=-2 hole in the east elevation is closed). projects/example-frontyard has one row, holly-corner at y=25.278, that sits 0.278 ft past the west elevation's 25 ft reach, so it snaps in by ~3 inches the first time it is dragged. That is the rule working: the plant is currently invisible in the west elevation. Left the data alone.","acceptance_criteria":"A plant cannot be dragged into a position where it vanishes from another view. Whichever way it is resolved, the rule is stated in AGENTS.md alongside the axis/mirror table.","notes":"OPEN QUESTION FOR THE USER (not a code bug): projects/example-frontyard's west elevation covers y 0..25 while its plan covers y 2..27, so the two disagree about the yard's north edge by 2 ft. holly-corner sits at y=25.278, outside the shared bounds, and will snap in by ~3.3 in the first time it is dragged (an ordinary undoable 'Moved plant' history entry). Left the data alone deliberately: widening the west view's extent or shifting its origin would slide its background photo out of alignment, and only the person who authored that photo knows which is right. Fix either by re-authoring the west view to cover y 2..27 or by nudging holly-corner to y\u003c=25.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T02:35:11Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:22:36Z","started_at":"2026-08-20T18:11:49Z","closed_at":"2026-08-20T18:13:23Z","close_reason":"Shared yard bounds implemented in src/render/yardBounds.js and consumed by both drag controllers via a new getBounds option; rule documented in AGENTS.md under Elevation orientation \u003e Yard bounds; tests/yardBounds.test.js covers the near-edge margin, the narrowing elevation, detail views, the non-overlapping fallback, and a plan view with a non-zero origin.","dependencies":[{"issue_id":"nl-tz7.12","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T21:35:10Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.11","title":"Convert repo project.json files to views[] and update AGENTS.md","description":"Rewrite projects/backyard/project.json and projects/example-frontyard/project.json into the views[] shape (the legacy reader stays in place for anyone else's project files).\n\nUpdate the Projects section of AGENTS.md: the viewFrom axis/mirror/depth table and the 'how to add a project' instructions both change, and originFt/extentFt replace bottomOffsetPx/leftOffsetPx/defaultPixelsPerInch. Note the new Setup mode as the preferred way to author a view. Mirror substantive edits into CLAUDE.md per the repo's own note that the two files are independent.","acceptance_criteria":"Both projects load and render pixel-identically to before the conversion. AGENTS.md and CLAUDE.md describe the views[] schema and Setup mode. Stashing one file back to the legacy shape still loads, proving the migration path.","notes":"Do this LAST — it rewrites the project files every other spec is tested against. Note the user has ALREADY converted projects/example-frontyard by hand through the Setup UI (uncommitted at handoff time); check git status before redoing it. projects/backyard is still in the legacy {plan, elevations[]} shape and is what tests-e2e/app.spec.js and the scratch fixture rely on, so converting it means re-checking those. AGENTS.md's Projects section still documents defaultPixelsPerInch and the two-elevation rule; both are gone. Also add a fourth view to a repo project here so the multi-panel path gets permanent e2e coverage — nl-tz7.4 could only verify it against a throwaway server.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:58:09Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:20:42Z","started_at":"2026-08-20T18:14:22Z","closed_at":"2026-08-20T18:20:42Z","close_reason":"projects/backyard/project.json converted to views[] — verified byte-for-byte equivalent by normalizing the old and new files and comparing (IDENTICAL), and locked in by a new test asserting 27 px/ft and the 80/100 px insets still fall where the legacy file put them. projects/example-frontyard was already converted by the user; added a fourth view to it, 'street-bed', a 9x6 ft detail at (3,3) with backgroundFrom:'plan', so the multi-panel and crop paths have permanent e2e coverage (new spec in tests-e2e/app.spec.js checks the panel count, the borrowed image, the crop percentages, that the uncropped plan keeps the stylesheet stretch, and that a plant inside the rectangle renders). AGENTS.md Projects section rewritten: views[] schema table, derived px/ft, Setup mode as the preferred authoring path, detail callouts, the legacy migration note, near-edge/mirroring wording, Scale-is-zoom, plus refreshed module list and e2e scratch-server guidance. CLAUDE.md multi-project paragraph mirrored.","dependencies":[{"issue_id":"nl-tz7.11","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:58:09Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.11","depends_on_id":"nl-tz7.3","type":"blocks","created_at":"2026-08-19T20:58:27Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.9","title":"backgroundFrom — crop a shared photo to a detail view's rectangle","description":"A crop view sets backgroundFrom: '\u003cviewId\u003e' instead of its own background, reusing the full plan photo cropped to its rectangle. Implemented in src/render/viewConfig.js as background-size / background-position arithmetic.\n\nThis is NOT the naive origin/fullExtent*100. CSS background-position: X% aligns the IMAGE's X% point with the CONTAINER's X% point, so the denominator is the leftover travel:\n\n  sizeX% = full.extentFt.width  / crop.extentFt.width  * 100\n  sizeY% = full.extentFt.height / crop.extentFt.height * 100\n  posX%  = (crop.originFt.x - full.originFt.x) / (full.extentFt.width  - crop.extentFt.width)  * 100\n  posY%  = 100 - (crop.originFt.y - full.originFt.y) / (full.extentFt.height - crop.extentFt.height) * 100\n\nposY is inverted because plan y grows north while CSS y grows down. Both denominators are zero when the crop equals the full extent — guard and fall back to 0%. A crop must set background-size explicitly per panel; it cannot inherit the 100% 100% stretch at styles.css:876.","acceptance_criteria":"A crop view with originFt {4,9} / extentFt {8,6} / backgroundFrom 'plan' shows exactly that patch of the plan photo, with the plants drawn over it landing on the same features they do in the full plan view. Unit test on the percentage math including the equal-extent guard.","notes":"Start here. The arithmetic is already worked out in the plan doc (~/.claude/plans/help-me-plan-for-jiggly-dragonfly.md) and is NOT the naive origin/fullExtent*100 — CSS background-position aligns the image's X% point with the container's X% point, so the denominator is the leftover travel:\n  sizeX% = full.extentFt.width / crop.extentFt.width * 100\n  posX%  = (crop.originFt.x - full.originFt.x) / (full.extentFt.width - crop.extentFt.width) * 100\n  posY%  = 100 - (same with y)   // plan y grows north, CSS y grows down\nBoth denominators are zero when the crop equals the full extent — guard and fall back to 0%. styles.css sets background-size: 100% 100% on .view, so a crop must set background-size explicitly per panel rather than inherit the stretch.\n\nConsumer is configureViews in src/render/viewConfig.js, which already resolves backgroundFrom's target by id (validation for an unknown id landed in nl-tz7.2). setupPanel.js already exposes a 'Borrow background from' select.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:58:08Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:10:29Z","started_at":"2026-08-20T18:07:12Z","closed_at":"2026-08-20T18:10:29Z","close_reason":"New src/render/backgroundCrop.js resolves a view's background image and, for a backgroundFrom detail view, the patch of it to show. Consumed twice: viewConfig.js sets background-size/-position on the panel (and clears both when a view stops borrowing, since panels are reused by id), and app.js passes the same rect to captureViewToPng as a sourceRect so the exported PNG shows the same patch instead of the whole photo stretched. The crop is derived by mapping the detail's corners through the SOURCE view's transform, which handles mirrored elevations and the plan y-flip for free. normalizeProjectConfig now rejects backgroundFrom across differing type/viewFrom, and the Setup panel only offers compatible sources. tests/backgroundCrop.test.js covers the worked example, the equal-extent and full-width guards, and both mirrored and unmirrored elevations. Export loop over views[] was already in place from nl-tz7.4; crop backgrounds were the missing piece.","dependencies":[{"issue_id":"nl-tz7.9","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:58:08Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.9","depends_on_id":"nl-tz7.4","type":"blocks","created_at":"2026-08-19T20:58:27Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.6","title":"Setup mode shell — third toolbar mode and view list CRUD","description":"applyMode currently computes locked = mode !== 'edit' and persists a boolean under native-landscaping-positions-locked. Replace with a mode string under a new key native-landscaping-mode, migrating the old boolean on first read (true-\u003eview, false-\u003eedit).\n\nAdd a third [data-mode='setup'] button and a #setupRow sibling of #editRow. Plant drag controllers stay locked whenever mode !== 'edit', so the setup controller owns the pointer in Setup mode.\n\n#setupRow contains: a view list with add / remove / reorder / duplicate, and a form for the selected view over label, sublabel, type, viewFrom, viewBox, extentFt, originFt, background / backgroundFrom. The numeric fields and the canvas are two views of the same state.","acceptance_criteria":"Mode persists across reloads and old localStorage values migrate. Adding a view appends a panel live with no reload; removing one removes its panel. Edits in the form update the canvas immediately. Save round-trips through POST /api/project. A Playwright spec covers the mode switch; any spec that SAVES must point PUBLIC_DIR at a scratch copy — the suite is read-only by design and would otherwise rewrite projects/backyard/project.json.","notes":"configureViews (src/render/viewConfig.js) reconciles panels by view id rather than rebuilding: a panel whose id is unchanged keeps its SVG element, so drag controllers stay bound and a live edit needs only 'mutate appState.project; configureViews(); render()'. That path is implemented but NOT yet exercised — nl-tz7.4 could not unit-test it (tests/helpers/fakeDom.js is an SVG-only stub and no jsdom is installed). Setup mode is its first real consumer: verify there that adding, removing, and reordering views does not orphan a drag controller.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:50Z","created_by":"John Syrinek","updated_at":"2026-08-20T04:05:19Z","started_at":"2026-08-20T03:47:13Z","closed_at":"2026-08-20T04:05:19Z","close_reason":"Setup mode shell landed: mode string under native-landscaping-mode with legacy boolean migration, third [data-mode=setup] button, #setupRow driven by src/interaction/setupPanel.js (view list with add/remove/reorder/duplicate plus a form over the selected view). Edits are validated through normalizeProjectConfig before being applied, so a rejected edit leaves the drawing on the last good state. Drag controllers gained destroy() and are rebuilt with the panels. 4 permanent e2e specs; save round-trip and drag-after-rebuild verified against a scratch PUBLIC_DIR.","dependencies":[{"issue_id":"nl-tz7.6","depends_on_id":"nl-tz7.5","type":"blocks","created_at":"2026-08-19T20:58:26Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.6","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:49Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.6","depends_on_id":"nl-tz7.4","type":"blocks","created_at":"2026-08-19T20:58:26Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"nl-tz7.7","title":"Setup overlay and handle controllers — grid, crop rect, ground line, near edge","description":"New src/render/setupOverlay.js (guides drawn into the selected view's SVG) and src/interaction/setupController.js (handle dragging), both on top of viewTransform.js.\n\n  - foot grid plus a dimension readout\n  - plan/crop views: a draggable rect with 8 handles -\u003e originFt / extentFt\n  - elevation views: a draggable ground line -\u003e originFt.y, and a draggable near-edge line -\u003e originFt.x\n\nReuse the patterns dragController.js already establishes: pointer capture, rAF-throttled re-render, setLocked, a generous hit radius (MIN_HITBOX_RADIUS_PX = 28). Rect-handle and line-handle controllers should land around 100-150 lines each. If that balloons past ~300, interact.js is the sanctioned fallback — it is small, SVG-capable, and sits beside dragController.js rather than replacing it. Do NOT reach for a canvas library.","acceptance_criteria":"Dragging the east elevation's ground line moves it live and plants' feet track it, no reload. Dragging a crop rect's handles updates originFt/extentFt and the numeric form in step. Overlay guides never appear outside Setup mode. tests/setupOverlay.test.js covers the guide geometry using tests/helpers/fakeDom.js.","notes":"originFt.x is the axis value at the view's NEAR edge, not the left edge. For mirrored directions (north, west) axisToX maps originFt.x to viewBox.width — the right-hand side. The draggable near-edge line must read/write against the mirrored edge for those two directions, or it will be built backwards. The plan doc's inline comment (\"axis value at left edge\") is only true for south/east. See createViewTransform in src/render/viewTransform.js and its inversion test.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:50Z","created_by":"John Syrinek","updated_at":"2026-08-20T05:29:48Z","started_at":"2026-08-20T05:15:51Z","closed_at":"2026-08-20T05:29:48Z","close_reason":"setupOverlay.js (foot grid, dimension readout, plan extent rect with 8 handles, elevation ground and near-edge lines) and setupController.js (pointer capture, rAF coalescing with a flush on pointerup, 28px hit radius) both built on viewTransform. Overlay draws only in Setup mode and only on the selected view. 12 unit tests via fakeDom plus 3 e2e. No new dependency; controller is 154 lines, well under the interact.js threshold.","dependencies":[{"issue_id":"nl-tz7.7","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:50Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.7","depends_on_id":"nl-tz7.6","type":"blocks","created_at":"2026-08-19T20:58:26Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-tz7.5","title":"POST /api/project — persist view config to project.json","description":"Add config persistence alongside the existing layout persistence.\n\n  - src/data/projectPaths.js: add configFile next to layoutFile / historyFile\n  - server.js: POST /api/project?project=\u003cslug\u003e. Re-validate the body through normalizeProjectConfig server-side (server already imports from src/data/), FORCE id from the directory name — never trust the client — and write atomically (tmp file + rename)\n  - src/data/persistence.js: client-side persistProjectConfig, injectable-fetch like its neighbours\n\nNo history/undo stack for config; the layout history machinery stays layout-only.","acceptance_criteria":"Saving from the browser rewrites projects/\u003cslug\u003e/project.json in views[] form with no default-valued keys, and a reload reproduces the state. Invalid bodies are rejected with 400 and a readable message. A client-supplied id is ignored. tests/projectPaths.test.js covers configFile; persistence tests cover persistProjectConfig.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:29Z","created_by":"John Syrinek","updated_at":"2026-08-20T02:47:18Z","started_at":"2026-08-20T02:45:21Z","closed_at":"2026-08-20T02:47:18Z","close_reason":"configFile added to resolveProjectPaths; POST /api/project re-validates through normalizeProjectConfig with the id forced from the directory, writes atomically via tmp+rename, and serializes with serializeProjectConfig. Client persistProjectConfig added. Verified against a scratch PUBLIC_DIR: 200 writes views[] with no default keys and is a serialize/normalize fixed point, a hostile body id is ignored, a non-uniform view is a 400 with a readable message, no tmp files left, repo projects/ untouched. No UI triggers it yet — that is nl-tz7.6.","dependencies":[{"issue_id":"nl-tz7.5","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:29Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.5","depends_on_id":"nl-tz7.2","type":"blocks","created_at":"2026-08-19T20:58:26Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"nl-5go.5","title":"Add a real phone breakpoint and fix the ragged layer chips","description":"styles.css has exactly ONE media query: max-width:960px. Below it, .view-toolbar__actions and .view-toolbar__control-group both keep min-width:280px (styles.css:82 and :91). At ~388px of content width two 280px boxes cannot share a row, so they wrap and stack. Confirmed no overflow results, but it is why the 5 layer chips land in three ragged right-aligned rows (105px tall).","design":"- Add a max-width:600px (or 640px) breakpoint for genuine phone layout.\n- Below it, drop the min-width:280px floors and switch justify-content:flex-end / align-items:flex-end to stretch or flex-start, so chips form a clean full-width wrap or a horizontal scroller instead of ragged right-alignment.\n- The 5 layer chips are 5 mutually-exclusive states of ONE setting - that is a segmented control or a \u003cselect\u003e, not 5 buttons. Their long labels ('Hide trees + sculptural', 'Hide through accents') are also what forces the wrapping.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:25Z","created_by":"John Syrinek","updated_at":"2026-08-19T22:33:20Z","started_at":"2026-08-19T22:31:06Z","closed_at":"2026-08-19T22:33:20Z","close_reason":"Added max-width:600px breakpoint; dropped the 280px min-width floors on toolbar actions/control-group; chips now flex into a clean 2-col grid instead of ragged right-aligned rows. Verified via iframe at 412px.","dependencies":[{"issue_id":"nl-5go.5","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:25Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-5go.6","title":"Trim the header and collapse species cards","description":"The document is 6450px at 412px wide, about 7 screens.\n\n- .page-header is 170px of the first screen and includes a .data-note paragraph about serving CSVs with 'npx serve' - developer instructions occupying ~20% of a phone's first screen.\n- .species-table is 4615px: 12 species x ~385px, because the \u003c=960px card layout renders all 10 columns as labelled rows per species.","design":"- Hide .data-note on mobile, or move it into the error-banner path, which is the only time it is actionable.\n- Shrink h1 from 1.6rem; consider folding the project picker into the same condensed bar as the month scrubber.\n- Make species cards collapsible: label + common name + botanical name by default, with height/width/growth form/sun/water/soil/bloom behind a \u003cdetails\u003e disclosure. That takes the block from ~4615px to roughly 900px.\n- Once the tap-a-plant sheet exists, the species table becomes a browse surface rather than the only detail surface, and can be a compact list.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:25Z","created_by":"John Syrinek","updated_at":"2026-08-19T22:41:37Z","started_at":"2026-08-19T22:37:54Z","closed_at":"2026-08-19T22:41:37Z","close_reason":"Hid dev-only .data-note and shrank h1 to 1.15rem on phone (\u003c=600px). Species cards now default to label+botanical+common name with a per-row 'Details' toggle revealing height/width/growth-form/sun/water/soil/bloom; table height for 12 species dropped from ~4615px to ~2061px collapsed. Desktop table (\u003e960px) unchanged - verified 10 columns, toggle cell display:none, in a 1400px iframe.","dependencies":[{"issue_id":"nl-5go.6","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:25Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-fqd","title":"Add Playwright end-to-end browser tests","description":"No automated browser tests exist; tests/ is all node --test unit tests with a fake DOM. Add Playwright e2e specs that boot server.js and assert on rendered SVG DOM (not screenshots) so agents need not drive Chrome manually.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T15:30:48Z","created_by":"John Syrinek","updated_at":"2026-08-19T15:33:25Z","started_at":"2026-08-19T15:30:51Z","closed_at":"2026-08-19T15:33:25Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-y8y","title":"Remove four-nerve daisies from backyard layout","description":"Two four-nerve-daisy-front rows in projects/backyard/planting_layout.csv should be deleted.","status":"closed","priority":2,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T15:30:48Z","created_by":"John Syrinek","updated_at":"2026-08-19T15:33:25Z","started_at":"2026-08-19T15:30:51Z","closed_at":"2026-08-19T15:33:25Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw.6","title":"Tests and docs for multi-project support","description":"Cover project config parsing, compass-direction elevation geometry, and server project path resolution. Update tests/persistence.test.cjs for the project-scoped URL, and update AGENTS.md (mirrored into CLAUDE.md) for the new on-disk layout.","acceptance_criteria":"npm test passes; AGENTS.md and CLAUDE.md describe projects/ and how to add a project.","status":"closed","priority":2,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:57:06Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4aw.6","depends_on_id":"nl-4aw","type":"parent-child","created_at":"2026-08-19T08:57:05Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4aw.4","title":"Project picker UI and ?project= routing","description":"Add a project selector to the header that navigates to ?project=\u003cslug\u003e; app.js resolves the active project from the query string, falling back to the default in projects/index.json.","acceptance_criteria":"Selecting a project reloads into that project; an unknown or missing slug falls back to the default with a visible notice.","status":"closed","priority":2,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T13:57:05Z","created_by":"John Syrinek","updated_at":"2026-08-19T14:10:19Z","closed_at":"2026-08-19T14:10:19Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4aw.4","depends_on_id":"nl-4aw","type":"parent-child","created_at":"2026-08-19T08:57:05Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-k07","title":"A yard narrowed to a corner by a short elevation raises nothing at boot","description":"resolveYardConflicts uses a strict overlap test, so it only fires when a primary elevation misses the plan entirely. example-frontyard, after nl-cgn, overlaps everywhere and raises nothing — yet its shared yard is x 0-9.37, y 0-14.35 out of a 29.89 x 30.31 plan, about 15%. The consequences are the same shape as the disjoint case, one notch less severe: the traced bed in features.json (x 11.45-20.27) is entirely outside the plantable yard, blackfoot-b at x 12.0 is outside it, and the east elevation draws no plants at all because all five sit at y 14.833 and its far edge is 14.349.\\n\\nSetup mode now reads the rectangle out (describeYardBounds, nl-cgn), so an author editing geometry sees it. Someone just loading the page does not.\\n\\nNote this may not be a code bug at all: the 9.37 ft and 14.35 ft extents came from the author's own Measure-a-known-length drags on those photos, and if either measurement is wrong the fix is re-measuring, not code. Decide that first.","acceptance_criteria":"Either the extents are re-measured and the shared yard covers the planted area, or a yard much smaller than its plan is surfaced to a viewer, not only to an author in Setup mode.","notes":"Also: a plant already sitting behind a newly-authored viewerAtFt is neither culled (plants never are) nor moved (the depth clamp only binds new drags), and since it is still inside the view's axis range it draws at nearest depth — on top of everything. Same visible symptom the wall bug had, with a plant. Reachable today on example-frontyard: set south's camera to 20 and all five plants at y 14.833 are behind it. Not a new class — they are already outside bounds.y.max of 14.349 — but the depth axis turns the failure from quiet (off-canvas) into loud (drawn in front).","status":"open","priority":3,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T13:29:44Z","created_by":"John Syrinek","updated_at":"2026-08-21T14:57:50Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-9j6","title":"example-frontyard's west elevation covers a y range the plan does not","description":"resolveYardBounds narrows the plan's rectangle by each primary elevation's axis range. In projects/example-frontyard, the plan covers y 14.83..45.14 while the 'west' view (viewFrom east, axis y) covers y 0..14.35 — the two do not overlap at all, so the inverted range falls back to the plan and the west elevation can draw none of the shared yard.\n\nConsequences are not specific to features: a plant dragged inside the shared yard is invisible in that elevation too, and a newly added yard feature lands off its canvas however it is placed. Found while fixing nl-avt.6's 'new feature not visible in other views' report — the general fix (anchor and size new shapes to resolveYardBounds) leaves this one project's west elevation unfixable because no yard coordinate is in both.\n\nEither the project's geometry is wrong (its west elevation was authored against a different origin) or resolveYardBounds should report a non-overlapping view rather than silently falling back. AGENTS.md calls example-frontyard a throwaway template, so fixing the config is probably right.","acceptance_criteria":"A coordinate inside resolveYardBounds is drawable in every primary view of example-frontyard, or the app says which view disagrees.","status":"closed","priority":3,"issue_type":"bug","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T03:30:18Z","created_by":"John Syrinek","updated_at":"2026-08-21T03:57:53Z","started_at":"2026-08-21T03:54:51Z","closed_at":"2026-08-21T03:57:53Z","close_reason":"Reported rather than repaired. resolveYardConflicts (src/render/yardBounds.js) returns each primary elevation whose axis range does not intersect the plan's; app.js shows it in #projectNotice at boot. For example-frontyard: 'View \"west\" covers y 0–14.3 ft, but the plan covers 14.8–45.1 ft — nothing in the yard can appear in both.' The geometry itself is NOT rewritten: the correct coordinate is one the author has to choose and the app cannot measure it, so guessing would silently move someone's yard. resolveYardBounds keeps its silent fallback on purpose — a drag needs some bound — the conflict is now surfaced alongside it. 2 unit tests in tests/yardBounds.test.js, including that end-to-end touching is not overlapping. Note the app's own touch-action/cursor precedent: this is the same shape of bug, a silent fallback that looks like it worked.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-avt.7","title":"Trace features over a calibrated photo, then hide it","description":"The authoring flow that makes this epic worth building, and the answer to 'setting up views is fiddly': upload a photo, calibrate it with the existing ruler, trace the house/fence/bed edges over it, then toggle the photo off or leave it on.\n\nThe photo becomes a tracing aid rather than the deliverable — accurate feet-based geometry, with the photo still available when showing the plan to someone. Needs a per-view photo visibility flag; the upload, the ruler, and the feature renderer all already exist by this point.","acceptance_criteria":"A view with both a photo and features can show either or both; hiding the photo leaves the drawn yard intact and needs no crop math.","status":"open","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:53Z","created_by":"John Syrinek","updated_at":"2026-08-21T01:53:53Z","dependencies":[{"issue_id":"nl-avt.7","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:53Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-avt.7","depends_on_id":"nl-avt.3","type":"blocks","created_at":"2026-08-20T20:53:55Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-avt.4","title":"Sky and ground fill as per-view properties","description":"Sky is not a shape and modelling it as one feels wrong immediately. Sky and ground fill are per-view presentation, so they belong on the view in project.json (skyColor / groundColor) alongside label and background — not in features.json, which holds yard geometry only. Ground fill paints below the view's ground line; sky paints above it.","acceptance_criteria":"A view can declare skyColor/groundColor, they survive a Setup-mode save, and a view that declares neither renders exactly as it does today.","status":"open","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:53:31Z","created_by":"John Syrinek","updated_at":"2026-08-21T01:53:31Z","dependencies":[{"issue_id":"nl-avt.4","depends_on_id":"nl-avt","type":"parent-child","created_at":"2026-08-20T20:53:30Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-8f9","title":"A borrowed background crop assumes the source photo fills its viewBox","description":"cropToCssBackground emits background-size as a percentage of the CONTAINER (1/cropWidth), which presumes the source photo covers the whole panel. Since backgrounds are now painted 'contain', that presumption is false whenever a source photo's aspect differs from its view's aspect: the photo letterboxes, but the crop still scales as if it did not, so a backgroundFrom detail view shows the wrong patch.\n\nPre-existing rather than introduced — under the old 'background-size: 100% 100%' the same mismatch stretched the source photo, so the crop rectangle was equally wrong, just wrong in a different way. Only correctly-authored sources (photo aspect == view aspect) are unaffected, which is why projects/backyard and example-frontyard's street-bed detail are fine and the crop tests pass.\n\nFix means letting the crop math account for the letterbox: compute the photo's drawn rectangle inside the panel from the photo's intrinsic aspect vs the view's, then express the crop relative to that rather than to the container. Needs the photo's intrinsic dimensions, which nothing currently loads.","acceptance_criteria":"A detail view borrowing from a source whose photo aspect differs from its view aspect shows the same patch of yard on screen as in the exported PNG.","status":"open","priority":3,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T01:11:54Z","created_by":"John Syrinek","updated_at":"2026-08-21T01:11:54Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-00f","title":"Uploaded backgrounds are orphaned if the user never saves views","description":"POST /api/view-background writes the photo to disk immediately, but the view's 'background' path only reaches project.json when Setup mode's *Save views* runs. Upload a photo, then close the tab without saving, and the file stays in projects/\u003cslug\u003e/img/ with nothing referencing it. supersededBackgrounds only collects earlier uploads for a view when a NEW upload to that same view succeeds, so an abandoned file is never collected.\n\nNot urgent — the files are small (well under 1 MB) and a later upload to the same view does clean up. Options: sweep unreferenced \u003cview\u003e-\u003chash\u003e.\u003cext\u003e files on project save, or upload to a staging name and promote it on save.","acceptance_criteria":"Uploading a background and then abandoning the session leaves no file that nothing references.","status":"open","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-21T00:44:17Z","created_by":"John Syrinek","updated_at":"2026-08-21T00:44:17Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-2p3","title":"example-frontyard is both a shipped test fixture and the user's live project","description":"Repo tests pin geometry and plant ids in projects/example-frontyard, but the same project is editable in the running app: Setup-mode Save rewrites project.json and a drag rewrites planting_layout.csv. On 2026-08-20 a Save dropped the 'street-bed' detail view, breaking tests/projectConfig.test.js and the crop e2e spec; the layout CSV was curated at the same time, removing rockrose-bed. Move the multi-panel/crop coverage onto the scratch fixture (tests-e2e/scratch-fixture.mjs already builds a throwaway root) or onto a project that is not user-editable, so ordinary use of the app cannot fail the suite.","acceptance_criteria":"No test in tests/ or tests-e2e/ asserts on geometry or plant ids that live in a project the user edits through the UI. Editing example-frontyard in the app and saving leaves npm test and npm run test:e2e green.","status":"closed","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T21:56:59Z","created_by":"John Syrinek","updated_at":"2026-08-21T02:23:45Z","closed_at":"2026-08-21T02:23:45Z","close_reason":"Both shipped-project tests moved off user-editable projects. tests/projectConfig.test.js now uses inline fixtures (LEGACY_BACKYARD for the migration parity test, PLAN_WITH_DETAIL for the crop-config test) — what was under test was normalizeProjectConfig all along, so the input belongs beside the assertions. The e2e crop spec moved onto a new 'detail-crop' scratch project whose fourth-view geometry is owned by tests-e2e/scratch-fixture.mjs. No test in tests/ or tests-e2e/ now asserts on geometry or plant ids from a project the user edits through the UI.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-3vs","title":"Verify the long-press clone/remove menu on a real touch device","description":"The context menu opens on the native contextmenu event, which on touch means long-press. CDP's Input.dispatchTouchEvent does not synthesize contextmenu, so tests-e2e/touch.spec.js cannot cover it either way — a hold delivers only pointerdown/pointerup/click. Two things worth checking on an actual phone: whether long-press still reaches the menu now that the SVG is touch-action:none in Edit mode, and whether handlePointerDown's event.preventDefault() suppresses it when the press lands on a plant. Not a known break; unverified. The touch route to Clone/Remove is covered: a tap in View mode opens the detail sheet, which carries both.","status":"open","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T18:29:35Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:29:35Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-jfm","title":"Drag and setup controllers both write svg.style.cursor","description":"Same shared-element bug as the touch-action one just fixed (both controllers own the same SVG and the last writer wins), but for cursor. applyMode unlocks the drag controller (cursor: grab), then syncSetupOverlay re-locks the setup controller, which resets it to default. So Edit mode never shows the grab cursor on desktop. Cosmetic only; touch-action was fixed by giving each controller its own class, and cursor wants the same treatment.","notes":"Now three writers, not two: src/interaction/featureController.js (nl-avt.6) also sets svg.style.cursor — in setLocked, release, and updateHoverCursor — and a plan SVG carries drag + setup + feature controllers. touch-action was handled correctly for the third one (is-features-enabled, mirroring the existing class-per-controller fix); cursor was not. Whatever fix lands has to cover all three.","status":"open","priority":3,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T18:29:34Z","created_by":"John Syrinek","updated_at":"2026-08-21T03:23:34Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-a7g","title":"Emptying a layout discards its history on reload","description":"persistLayout now accepts an empty plants array, so removing the last plant does reach disk. But loadLayoutHistory -\u003e findHistoryEntryIndexByLayout returns -1 for an empty layout CSV (it bails on a blank string), so the next page load reports 'History diverged from planting_layout.csv' and throws the history stack away. Degrades quietly; only reachable by removing every plant.","status":"open","priority":3,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T18:12:42Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:12:42Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.13","title":"Setup mode authors feet; viewBox is derived","description":"Resolved the open question in the plan doc's Design section 8 ('derive viewBox from extentFt — decide when building bead 6').\n\nThe first cut of the setup form locked extentFt to the viewBox's aspect ratio, so typing a height silently rewrote the width — you could never say '30 ft by 10 ft'. Wrong value derived.\n\nNow extentFt is authored freely in both directions and viewBox = extentFt * a per-view resolution (px per ft). Aspect ratio is whatever the extent says, and the non-uniform view createViewTransform rejects is unreachable from the form rather than merely guarded against. The two pixel fields collapsed into one 'Resolution (px per ft)' knob, with the derived drawing size shown as a hint.\n\nnormalizeProjectConfig and serializeProjectConfig still read and write an authored viewBox, so hand-edited project.json files are unaffected.","acceptance_criteria":"Setting Width (ft) then Height (ft) leaves both at what was typed; covered in tests-e2e/app.spec.js.","status":"closed","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T04:27:40Z","created_by":"John Syrinek","updated_at":"2026-08-20T04:27:41Z","closed_at":"2026-08-20T04:27:41Z","close_reason":"Landed with the drag-coverage commit.","dependencies":[{"issue_id":"nl-tz7.13","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T23:27:40Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-4im","title":"Upload background images through the UI","description":"DEFERRED from nl-tz7. Setup mode expects background images to be dropped into projects/\u003cslug\u003e/img/ by hand, with the relative path typed into the form. A file-picker upload endpoint would remove that step.\n\nNeeds multipart handling in server.js (which is currently JSON-only), an allowlist of image types matching MIME_TYPES, a size cap, and the same path-traversal guard resolveProjectPaths already applies.","notes":"Follow-up 2026-08-20, reported against the shipped feature: an uploaded photo was distorted. Root cause was pre-existing — .view painted backgrounds 'background-size: 100% 100%', which scales a photo differently on each axis whenever its aspect differs from the view's. It stayed invisible because every projects/backyard photo is 800x600 against an 800x600 viewBox.\n\nFixed in styles.css by painting 'contain' instead, and by capping the panel's WIDTH rather than its height (width: min(100%, calc(70vh * var(--view-aspect-ratio)))) — with width:100% a max-height constrains both axes, drops aspect-ratio, and reintroduces the stretch. Per the user's instruction the photo does NOT reshape the view: extentFt is untouched by an upload.\n\nThe crop branch in viewConfig.js sets background-size explicitly and was deliberately left alone; see the follow-up bead about backgroundFrom crops under letterboxing.","status":"closed","priority":3,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:58:19Z","created_by":"John Syrinek","updated_at":"2026-08-21T01:12:35Z","started_at":"2026-08-21T00:33:16Z","closed_at":"2026-08-21T00:44:17Z","close_reason":"Closed","dependencies":[{"issue_id":"nl-4im","depends_on_id":"nl-tz7.6","type":"blocks","created_at":"2026-08-19T20:58:28Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-avt","title":"Yard feature drawing (beds, hardscape, house footprint)","description":"DEFERRED — deliberately out of scope for the Viewport Setup Mode epic (nl-tz7), and nothing in that epic blocks this.\n\nBeds, borders, hardscape, house footprint, paths, and existing trees are yard geometry in FEET shared across every view — different in kind from per-view presentation config, so they do not belong in project.json. They get their own file, projects/\u003cslug\u003e/features.json, their own render module drawn beneath plants, and their own endpoint.\n\nRendering a feature into an elevation reuses viewTransform / resolveElevationOrientation, so the mirror/offset math stays in exactly one place. Depends on nl-tz7.1 landing viewTransform.js.","design":"APPROACH: one yard model in FEET, projected into every view — never a drawing per view.\n\nDrawing a background per view is the cheap version and it is a trap: the house\ngets drawn three times, the three disagree, and it buys nothing on occlusion.\nOne model projected everywhere is where all the leverage is, and it is what the\noriginal description already calls for.\n\nAccept the conceptual cost up front: this is a small 2.5D model, not a drawing\ntool. Every feature carries a FOOTPRINT and a HEIGHT, because it has to appear\nin both a plan and an elevation. That is the part that bites during design\nrather than during coding.\n\n--- PRIMITIVES (three is enough for a real yard) ---\n\nsurface  flat, zero height: driveway, sidewalk, gravel bed, lawn, bed outline.\n         Plan: filled polygon. Elevation: a thin band at ground level across\n         its axis span.\nwall     an extruded line: fence, retaining wall, garden edge.\n         Plan: a thick line along its path. Elevation: a rectangle from base to\n         base+height over its axis span.\nbox      an extruded polygon: house, shed, raised planter.\n         Plan: the footprint. Elevation: a silhouette rectangle spanning its\n         axis extent, base to base+height.\n\nSilhouettes are deliberately crude rectangles. That is the point — this is the\nin-app version of the 3-to-5 \u003crect\u003e SVGs example-frontyard already ships.\n\nSKY IS NOT A FEATURE. Sky and ground fill are per-view presentation, not yard\ngeometry, so they belong on the view in project.json (skyColor / groundColor),\nnot in features.json. Modelling sky as a shape will feel wrong immediately.\n\n--- SCHEMA: projects/\u003cslug\u003e/features.json ---\n\n{\n  \"features\": [\n    {\n      \"id\": \"house\",              // slug, unique, same pattern as view ids\n      \"type\": \"box\",              // surface | wall | box\n      \"label\": \"House\",\n      \"footprintFt\": [ {\"x\":0,\"y\":28}, ... ],   // polygon, yard feet (wall uses pathFt)\n      \"baseFt\": 0,                // ground height at its base; lets a feature sit on a step\n      \"heightFt\": 22,             // omitted/0 for surface\n      \"style\": { \"fill\": \"#e8e4dc\", \"stroke\": \"#c9c3b8\", \"strokeWidthFt\": 0.1 }\n    }\n  ]\n}\n\nStrokes are authored in FEET like everything else, so a feature keeps its\nweight when a view is rescaled. Normalize/serialize mirrors projectConfig.js:\nreject non-finite coordinates, default the style, drop defaults on write.\n\n--- PROJECTION (the crux) ---\n\nPlan:      map each footprint vertex through transform.planToViewBox. Direct.\nElevation: project the footprint onto the view's horizontal axis to get\n           [axisMin, axisMax], map both through axisToX; vertical runs from\n           heightToY(baseFt) to heightToY(baseFt + heightFt).\nDepth:     a feature's extent along the view's DEPTH axis, for sorting.\n\nGo through viewTransform and resolveElevationOrientation for every one of\nthese. Do NOT do the subtraction in feet by hand — north and west are mirrored\nand yard y grows north while image y grows down. AGENTS.md's axis/mirror table\nis the authority and the parity tests in tests/elevationOrientation.test.js are\nthe template.\n\n--- DRAW ORDER: a refinement of this epic's original design ---\n\nThe description says \"drawn beneath the plant layer in every view\". That is\nright for a PLAN and wrong for an ELEVATION.\n\nPlan:      features beneath plants, always. Sorted among themselves by array\n           order (authoring order is the z-order).\nElevation: features and plants INTERLEAVED, sorted together far-to-near by\n           depth. This is the whole payoff — a fence at y=30 with height 6\n           actually hides the plant behind it, which the photo approach could\n           never do without per-pixel depth.\n\nThat interleave is the one integration point that is not purely additive:\nrenderViews currently sorts plants alone (orderTopViewPlantStates for plan,\nelevationViews for the rest). It has to sort one combined list for elevations.\n\nFull per-view layering, bottom to top:\n  1. sky colour (view-level)\n  2. photo background, if the view has one and it is visible\n  3. ground fill below the ground line (view-level)\n  4. features + plants, interleaved by depth in elevations; features under\n     plants in plan\n  5. overlays — setup handles, ruler, labels\n\nFeatures sit ABOVE the photo, which is what makes tracing work.\n\n--- WHAT THIS REMOVES ---\n\n* Calibration. A drawn-only view needs none: you author in feet, so pxPerFt is\n  already known. \"Measure a known length\" stays, but only for photo views.\n* Detail crops. A drawn view re-renders its vectors at its own extentFt — no\n  crop rectangle, no background-size percentages, no drawImage source rect.\n  nl-8f9 is irrelevant for drawn views, and backgroundFrom stays photo-only.\n* The occlusion problem that made the perspective/photo option open-ended.\n\n--- EDITOR SCOPE: edit in the PLAN view only ---\n\nThe single biggest scope saver, and it is well justified: footprints live in\nplan space, and a height is a number in a form field, not a drag. Elevations\nare DERIVED and read-only.\n\nMVP: select a feature, move it, drag a vertex, add a rect/line, delete,\nreorder. Reuse createSetupController's proven pattern — pointer capture, rAF\nthrottle, MIN_HITBOX_RADIUS_PX, candidate reported through onChange for the app\nto validate. Do not invent a second interaction model.\n\n--- AUTHORING FLOW: the hybrid, and the reason to build this at all ---\n\n  upload a photo -\u003e calibrate it with the ruler -\u003e TRACE the house, fence, and\n  bed edges over it -\u003e toggle the photo off (or leave it on)\n\nThe photo becomes a tracing aid rather than the deliverable. Tracing over an\nalready-calibrated photo is a far better gesture than drawing blind in feet,\nand it directly answers the \"authoring views is fiddly\" complaint that started\nthis. Needs a per-view photo visibility flag; everything else already exists.\n\n--- RISKS ---\n\n* Editor scope creep. This is an app inside the app. Ship the smallest\n  primitive set that draws the real front yard, then stop.\n* Undo. There would be three stacks (layout has one, project config has none).\n  Simplest honest answer: no history for features, same as project.json.\n* Curves. Polygons only in v1; a curved bed is approximated with vertices.\n* Sloped ground. baseFt handles discrete steps. True slopes are out of scope\n  and should stay that way.\n* Persuasiveness. Crude shapes convince a person less than a photo does. This\n  is the one axis where photos genuinely win, and it is exactly what the\n  photo-underlay hybrid is for.\n\n--- TESTING ---\n\n* Projection is pure: unit-test it like backgroundCrop and yardBounds, with a\n  parity case per compass direction including the mirrored ones.\n* Any e2e that saves features must use the scratch fixture and take its OWN\n  project in SCRATCH_PROJECTS — a spec pointed at a project not in that list\n  silently falls back to the default and asserts against the wrong yard.\n\n--- NOT DOING ---\n\nPerspective rendering. It breaks the uniform-pxPerFt invariant that\nviewTransform.js enforces, turns pxPerFt into pxPerFtAt(point) across ~20\ncallers, forces backgrounds out of CSS into canvas for screen and export\nalike, and leaves depth/occlusion against photo content open-ended. This epic\ngets the eye-level-ish result people want at a fraction of the cost, and keeps\nthe coordinate system that makes plant SIZE meaningful (topView.js draws a\nplant at toPx(plant.width)/2 — feet are what make the drawing answer \"will this\nfit\").","status":"open","priority":3,"issue_type":"epic","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:58:19Z","created_by":"John Syrinek","updated_at":"2026-08-21T01:53:11Z","dependencies":[{"issue_id":"nl-avt","depends_on_id":"nl-tz7","type":"blocks","created_at":"2026-08-19T20:58:34Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.10","title":"Export bundle loops over views[]","description":"captureViewToPng (src/export/viewCapture.js) is per-SVG and needs no change, but its caller at src/app.js:317 enumerates the three panels by hand ({ view: project.plan, svg: svgRefs.topSvg, fileName: 'plan-view.png' }, ...). Move that loop to views[].\n\nCrop views ARE included in the export — a detail callout is exactly the kind of thing worth having as a PNG. File names come from the view id: plan-view.png, shade-bed-view.png, east-view.png.","acceptance_criteria":"Exporting a project with four views yields four PNGs named from view ids, each with its background composited. Existing three-view export output is unchanged apart from any id-derived renaming.","notes":"Partly done already: nl-tz7.4 replaced the hand-enumerated export panel list with a loop over the cloned viewPanels, so crop views are included automatically. That also renamed the files — elevations went from \u003cid\u003e-elevation.png to \u003cid\u003e-view.png, matching the plan doc's plan-view.png / shade-bed-view.png scheme. What remains here is whatever naming or per-view export options this bead still wants on top.","status":"closed","priority":3,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:58:09Z","created_by":"John Syrinek","updated_at":"2026-08-20T18:10:29Z","started_at":"2026-08-20T18:10:28Z","closed_at":"2026-08-20T18:10:29Z","close_reason":"New src/render/backgroundCrop.js resolves a view's background image and, for a backgroundFrom detail view, the patch of it to show. Consumed twice: viewConfig.js sets background-size/-position on the panel (and clears both when a view stops borrowing, since panels are reused by id), and app.js passes the same rect to captureViewToPng as a sourceRect so the exported PNG shows the same patch instead of the whole photo stretched. The crop is derived by mapping the detail's corners through the SOURCE view's transform, which handles mirrored elevations and the plan y-flip for free. normalizeProjectConfig now rejects backgroundFrom across differing type/viewFrom, and the Setup panel only offers compatible sources. tests/backgroundCrop.test.js covers the worked example, the equal-extent and full-width guards, and both mirrored and unmirrored elevations. Export loop over views[] was already in place from nl-tz7.4; crop backgrounds were the missing piece.","dependencies":[{"issue_id":"nl-tz7.10","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:58:08Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.10","depends_on_id":"nl-tz7.4","type":"blocks","created_at":"2026-08-19T20:58:27Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-tz7.8","title":"Two-point ruler calibration from the background photo (optional)","description":"OPTIONAL — the user asked to explore photo calibration without committing to it. Ship the rest of the epic first; this is a recommendation because it is unusually cheap, not a requirement. Dropping it leaves the numeric extentFt field doing the same job.\n\nThree gestures cover the whole of 'calibrate against a photo', with no homography, no new data model, and no OpenSeadragon:\n  - drag across a known length in the photo, type the real length -\u003e solves pxPerFt, hence extentFt\n  - click the ground line -\u003e originFt.y   (covered by nl-tz7.7)\n  - click the near edge   -\u003e originFt.x   (covered by nl-tz7.7)\n\nThis bead is the ruler only. styles.css:876 is background-size: 100% 100%, so viewBox units map proportionally onto the image and the measurement is well-posed.","acceptance_criteria":"Dragging the ruler across a fence panel of known length and entering that length updates extentFt, and the view re-scales so the plan matches the photo. Unit-tested math for the solve.","status":"closed","priority":3,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-20T01:57:50Z","created_by":"John Syrinek","updated_at":"2026-08-20T23:00:07Z","started_at":"2026-08-20T22:10:14Z","closed_at":"2026-08-20T23:00:07Z","close_reason":"Two-point ruler shipped. resolveRulerCalibration + measureRuler in src/render/setupOverlay.js solve extentFt from a drag across the background photo and a typed real length; setupController gains setRuler()/onRuler (the armed pointer measures instead of moving handles); setupPanel gains the Measure a known length toggle, the px/ft readout, and the length field; app.js holds the segment in appState.ruler and clears it before the rebuild. Unit tests cover the solve on a plan and on a mirrored (north) elevation — asserting the near edge and ground line keep their fraction of the photo — plus diagonal equivalence and the misclick/nonsense-length guards. One e2e spec measures half the plan panel and asserts Width (ft) doubles the typed length. Two bugs found by that spec and fixed: the pointer's last position is now held outside the rAF-throttled payload (a release after a flushed frame measured zero), and the panel only learns the measurement on drag end (growing it mid-gesture shifted the canvas under the pointer). AGENTS.md documents the gesture and the anchoring rule.","dependencies":[{"issue_id":"nl-tz7.8","depends_on_id":"nl-tz7.7","type":"blocks","created_at":"2026-08-19T20:58:27Z","created_by":"John Syrinek","metadata":"{}"},{"issue_id":"nl-tz7.8","depends_on_id":"nl-tz7","type":"parent-child","created_at":"2026-08-19T20:57:50Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-5go.7","title":"Mobile polish: tap targets, resize thrash, numeric scale input","description":"Smaller items from the mobile review.\n\n- Tap targets below the 44px guideline: chips 31px tall, #projectSelect 29px, .view-panel__toggle ~24px. Pill switches at 46px are fine.\n- window.addEventListener('resize', render) (src/app.js:520): Android's address bar showing/hiding and the keyboard opening on #scaleInput both fire resize, causing a full re-render of 192 plant groups x 3 views mid-scroll. Debounce it, or compare dimensions before re-rendering.\n- #scaleInput is type='number' with step='0.01' - opens a numeric keypad and is fiddly on touch. On mobile the slider alone (plus the per-view '1 ft = N px' badge) is enough.\n\nNOT a problem, checked and confirmed fine: the drag hitbox. MIN_HITBOX_RADIUS_PX = 28 (dragController.js:3) is multiplied by scaleFactor = viewBox / rect (:362, :418), so it is 28 SCREEN px regardless of render size. No small-screen inversion; a 56px-diameter target is acceptable.","status":"closed","priority":3,"issue_type":"task","assignee":"John Syrinek","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T19:02:26Z","created_by":"John Syrinek","updated_at":"2026-08-19T22:43:52Z","started_at":"2026-08-19T22:42:41Z","closed_at":"2026-08-19T22:43:52Z","close_reason":"Tap targets (chips, view-panel__toggle, projectSelect) bumped to 44px min-height on phone (\u003c=600px). Resize handler debounced 150ms and skips re-render when innerWidth is unchanged, so address-bar show/hide and keyboard-open no longer force a full 192-group x3-view re-render. #scaleInput (numeric keypad) hidden on phone; slider + per-view badge remain. Verified in-browser at 412px.","dependencies":[{"issue_id":"nl-5go.7","depends_on_id":"nl-5go","type":"parent-child","created_at":"2026-08-19T14:02:25Z","created_by":"John Syrinek","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-8fm","title":"Layout history seeds bypass parsePlantLayoutCsv validation","description":"appState.plants can come from layoutHistoryInstance.getCurrentPlants() (src/app.js), which is seeded from the server's layout-history.json. Those entries are plain plant arrays and never pass through parsePlantLayoutCsv, so the duplicate-id constraint added in nl-92d does not cover them. Not reachable today: cloning routes through buildCloneId, CSV load now throws, and a scan of projects/example-frontyard/layout-history.json (6 entries) found no duplicates. Worth closing if a future path writes history directly.","acceptance_criteria":"Either history seeds are validated on load, or a note records why the path is safe by construction.","status":"closed","priority":3,"issue_type":"task","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T17:51:25Z","created_by":"John Syrinek","updated_at":"2026-08-19T22:49:52Z","closed_at":"2026-08-19T22:49:52Z","close_reason":"Not worth pursuing; path is safe by construction (no known way to write history without going through validated CSV load/clone flow)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"nl-92d","title":"Duplicate plant id in backyard planting_layout.csv","description":"projects/backyard/planting_layout.csv has two rows sharing the id 'four-nerve-daisy-front' (lines 26 and 27). Ids are used to look up plants for dragging, cloning, and hover/target highlighting, so the second row is effectively unreachable — dragging either silhouette moves the first match. Pre-existing, found while verifying the multi-project change; not caused by it.","acceptance_criteria":"Every id in planting_layout.csv is unique; consider validating uniqueness in parsePlantLayoutCsv.","status":"closed","priority":3,"issue_type":"bug","owner":"john.syrinek@gmail.com","created_at":"2026-08-19T14:10:18Z","created_by":"John Syrinek","updated_at":"2026-08-19T17:46:52Z","closed_at":"2026-08-19T17:46:52Z","close_reason":"Duplicates already removed by nl-y8y; parsePlantLayoutCsv now throws on repeated ids, with unit tests.","dependency_count":0,"dependent_count":0,"comment_count":0}
